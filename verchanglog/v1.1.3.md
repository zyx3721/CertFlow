# CertFlow v1.1.3 版本更新日志

**发布日期**: 2026-09-23  
**提交数量**: 8 次提交  
**版本类型**: 功能新增与安全增强

---

## 📋 版本概述

本版本聚焦登录安全与跨项目配置口径统一：新增登录失败锁定机制（连续密码错误达到阈值后锁定，内置管理员 admin 不受限），锁定次数与锁定时长纳入平台「安全时效」配置；找回密码重置成功后自动解除登录失败锁定；前端登录态切换为 localStorage 持久存储，实现同一浏览器内新建标签页与重启浏览器自动登录；SSR 品牌直出的后端地址环境变量与兄弟项目统一为 `SSR_API_ORIGIN`；同时移除认证库中指向失效接口的死代码。

---

## ✨ 新增特性

- **登录失败锁定**：同一账号连续密码失败达到「安全时效」配置的锁定次数后返回 429 并提示剩余等待分钟数，锁定时长内即使密码正确也会被拒绝，登录成功或锁定窗口过期后重新计数，内置管理员 admin 不受限；仅凭据错误计数，企业微信登录不涉及
- **锁定参数平台化配置**：设置页「安全时效」区块新增「登录失败锁定次数」（3-10 次）与「登录锁定等待时长」（1-10 分钟）两个配置项，后端判定实时读取设置键值，保存时范围校验
- **找回密码联动**：找回密码重置成功后自动解除该账号登录失败锁定（该账号此前的会话吊销能力保持不变）
- **审计增强**：登录锁定拒绝写入审计日志；找回密码重置审计详情按实际清除行数追加解锁说明
- **浏览器持久登录**：前端登录态由 sessionStorage 切换为 localStorage（含 PKI 客户端的令牌读取同步切换），同一浏览器新建标签页、重启浏览器均可自动登录；无痕模式与其他浏览器不共享登录态，登录态仅在服务端会话过期（默认 12 小时，401）后失效

---

## 🐛 Bug 修复

- 移除认证库 `auth.ts` 中指向不存在接口 `/api/auth/change-password` 的死代码 `changePassword`（实际接口为 `/api/v1/users/change-password`，生效实现在 `pki.ts`）

---

## 🚀 性能优化

本版本无独立性能优化项。

---

## 🔧 重构优化

- SSR 品牌直出的后端地址环境变量由 `CERTFLOW_SSR_API_ORIGIN` 统一为 `SSR_API_ORIGIN`，代码、模板与文档同步改名，与兄弟项目配置口径一致

---

## 📚 文档更新

- `docs/manual.md`：登录接口补充锁定行为说明、找回密码确认接口补充解锁语义、SSR 变量名与解析顺序同步
- `backend/.env.example` / `deploy/.env.example`：同步变量调整

---

## 🔄 API 变更

- `POST /api/v1/auth/login`：新增 429 锁定响应（达到锁定次数时返回剩余等待分钟数提示）
- `POST /api/v1/auth/password-reset/confirm`：行为增强，重置成功后解除该账号登录失败锁定

---

## 📊 数据库变更

新增迁移 `005_login_failure_log.sql`：创建 `login_failure_log` 表（`username` + `failed_at`）与用户名索引，用于登录失败锁定计数；不改其他表结构，无数据迁移。

---

## 🔄 升级说明

- 直接替换二进制或更新镜像即可，迁移随启动自动执行，无需手工执行 SQL
- 行为变化提示：登录失败锁定默认开启（5 次锁 2 分钟），可在设置页「安全时效」中调整；浏览器关闭不再清除登录态（localStorage 持久存储），需要退出请使用页面登出；找回密码重置成功后该账号所有已登录设备将被踢下线并解除锁定
- SSR 品牌直出如此前配置过 `CERTFLOW_SSR_API_ORIGIN` 环境变量，请改为 `SSR_API_ORIGIN`；未配置过则无需任何操作

---

## 👥 贡献者

- Jerion (416685476@qq.com)

---

## 📄 许可证

MIT License

---

**完整提交记录**: 8 次提交  
**开发周期**: 2026-09-22 至 2026-09-23 (2 天)
